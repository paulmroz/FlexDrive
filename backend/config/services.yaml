# yaml-language-server: $schema=../vendor/symfony/dependency-injection/Loader/schema/services.schema.json

# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.
# See also https://symfony.com/doc/current/service_container/import.html

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.
        bind:
            $environment: '%kernel.environment%'
            $webhookSecret: '%env(resolve:STRIPE_WEBHOOK_SECRET)%'
            $stripeSecretKey: '%env(resolve:STRIPE_SECRET_KEY)%'
            $stripeSuccessUrl: '%env(STRIPE_SUCCESS_URL)%'
            $stripeCancelUrl: '%env(STRIPE_CANCEL_URL)%'
            $cleanupThreshold: '%env(CLEANUP_ABANDONED_THRESHOLD)%'
            $advisorSystemPrompt: '%env(ADVISOR_SYSTEM_PROMPT)%'
            $extractorSystemPrompt: '%env(EXTRACTOR_SYSTEM_PROMPT)%'

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'
        exclude:
            - '../src/Kernel.php'
            - '../src/**/Tests/'
            - '../src/**/Domain/Entity/'
            - '../src/**/Domain/ValueObject/'
            - '../src/Shared/Domain/ValueObject/'



    # add more service definitions when explicit configuration is needed
    # please note that last definitions always *replace* previous ones

    redis_client:
        class: Redis
        factory: ['Symfony\Component\Cache\Adapter\RedisAdapter', 'createConnection']
        arguments:
            - '%env(REDIS_URL)%'

    Redis:
        alias: 'redis_client'

    App\Shared\Infrastructure\EventListener\DomainEventListener:
        arguments:
            $eventBus: '@event.bus'

    App\AuditLog\Infrastructure\Middleware\AuditLogMiddleware:
        arguments:
            $commandBus: '@command.bus'

    # Listen to JWT creation events (to inject user ID into JWT payload)
    App\User\Infrastructure\EventListener\JWTCreatedListener:
        tags:
            - { name: kernel.event_listener, event: lexik_jwt_authentication.on_jwt_created, method: onJWTCreated }
